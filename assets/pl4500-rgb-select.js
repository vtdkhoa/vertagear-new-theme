// update big image
const updateBigImageForRgbOption = () => {
  namShowBigImg();

  // Dont show rgb images
  
  // const { chairColor, options } = namProductRgbCartData;
  // const rgbImages = namProductShopify.images.filter((i) =>
  //   i.includes(`${chairColor.replace("/", "-")}-rgb`)
  // );
  // const currentBigImage = document.getElementById("namProductBigImage");

  // const [upgrade, logo, bottom] = options;
  // const isUpgradeSelected = upgrade.isSelected;
  // const isLogoSelected = isUpgradeSelected && logo.isSelected;
  // const isBottomSelected = bottom.isSelected;

  // if (isUpgradeSelected) {
  //   currentBigImage.src = rgbImages.find((i) => i.includes("rgb-only-upgrade"));
  // }
  // if (isLogoSelected) {
  //   currentBigImage.src = rgbImages.find((i) => i.includes("rgb-upgrade-logo"));
  // }
  // if (isBottomSelected) {
  //   currentBigImage.src = rgbImages.find((i) => i.includes("rgb-bottom"));
  // }
  // if (isUpgradeSelected && isBottomSelected) {
  //   currentBigImage.src = rgbImages.find((i) =>
  //     i.includes("rgb-upgrade-bottom")
  //   );
  // }
  // if (isLogoSelected && isBottomSelected) {
  //   currentBigImage.src = rgbImages.find((i) =>
  //     i.includes("rgb-logo-bottom")
  //   );
  // }
  // if (!isUpgradeSelected && !isLogoSelected && !isBottomSelected) {
  //   currentBigImage.src = namProductBigBaseImgSrc;
  // }
};
